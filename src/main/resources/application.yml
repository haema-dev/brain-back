server:
  port: 8080

spring:
  datasource:
    url: ${BRAIN_DB_URL}
    username: ${BRAIN_DB_USERNAME}
    password: ${BRAIN_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect